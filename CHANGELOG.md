# Changelog

## Unreleased

- Fixed the GUI demo to run a deterministic bundled replay predicate with temporary output.
- Fixed the GUI test action to run compilation and the actual unit suite.
- Fixed documented CLI options after the trace path being misread as predicate arguments.
- Added CI coverage for both real offscreen PySide6 actions.

## 0.1.0 - 2026-08-24

- Added cached delta debugging for JSONL traces with external replay predicates.
- Added timeout, run-limit, exact-exit-code, and machine-readable result controls.
